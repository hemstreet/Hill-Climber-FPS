using UnityEngine;

namespace Score
{
    public class Altitude : MonoBehaviour {

        // Use this for initialization
        void Start () {
            // Get altitude rewards
            // draw lines on map for their goals
        }

        // Update is called once per frame
        void Update ()
        {
            float zAxis = gameObject.transform.position.z;
        }
    }
}
